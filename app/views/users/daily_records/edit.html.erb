<div class="container">
  <div class="row">
    <div class="col-lg-9">
      <h2 class="daily-header"><b>新規登録</b></h2>
      <%= render "error", daily_record: @daily_record %>

        <div class="row">
          <%= form_with model: @daily_record, url: daily_record_path(@daily_record), local: true do |f| %>
            <%= render "form", f: f, daily_record: @daily_record, edit_flag: true %>
          <% end %>
        </div>
    </div>
  </div>
</div>